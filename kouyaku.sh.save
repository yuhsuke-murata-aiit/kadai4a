#!/bin/bash

# 引数チェック

echo $1 $2
if [ $# -ne 2 ]; then
    cat <<EOF
Usage: sudo $0 <valie_1> <value_2>
EOF
    exit 1
fi

# 入力ファイルチェック
#if [ -f ./inputFile ]; then
#    echo "inputFile Not Found"
#    exit 1
#fi

if[[! "$1" =~ ^[0-9]+$ || ! "$1" =~ ^[0-9]+$ ]]; then

fi



a=$1
b=$2

# a < n の場合、abの値を入れ替え
if [ a -lt b ]; then
    temp=a
    a=b
    b=a
fi

# ユークリッドの互除法で最大公約数を求める
while [ b -ne 0 ];
do
    temp=a mod b
    a=b
    b=temp
    echo $a $b
done

echo $a

